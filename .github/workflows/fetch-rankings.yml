name: Fetch Rankings
on:
  schedule:
    - cron: '0 0 * * *' # Runs daily at midnight UTC
  workflow_dispatch: 

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Essential so the Action has permission to save the file
    steps:
      - uses: actions/checkout@v3
      - name: Fetch and Save Data
        run: |
          mkdir -p data
          # This is the direct, unauthenticated endpoint for FantasyCalc data
          curl -sS "https://api.fantasycalc.com/values/current?isDynasty=false&numQbs=1&numTeams=12&ppr=1" > data/rankings.json
      - name: Commit and Push
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add data/rankings.json
          git commit -m "Automated update: $(date +'%Y-%m-%d')"
          git push
